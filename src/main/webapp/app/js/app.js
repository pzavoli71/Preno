/**
 * Questa classe rappresenta il javascript standard dello specifico applicativo.
 * Contiene i settaggi per applicativo su JQCombo, JQWindows, immagini tree view ed
 * altre impostazioni.
 * <br/>
 * È registrata nel namespace CIS ma è accessibile tramite il nome 
 * globale standard <strong>AppGlob</strong> .
 *
 * @class javascript standard dello specifico applicativo.
 * @constructor
 * @see {AppGlob}
 */

;if (typeof (AppGlob) === "undefined")
	AppGlob = {};